# TM1 Browserless API Notes

The previous repo-local browser automation scripts were retired with the agent runtime cleanup. Do not reference or recreate old local script paths by default.

If TM1 browserless automation is needed again, rebuild it as a deliberate web/backend integration with:

- current Ticketmaster API/session documentation
- explicit secrets handling
- dry-run and live-run separation
- durable audit logs
- tests or a manual verification runbook

Treat older TM1 automation notes in the git history as historical reference only.
