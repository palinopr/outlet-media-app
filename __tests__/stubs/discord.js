/**
 * Minimal stub for discord.js — used by vitest so agent tests can resolve
 * the package without installing the full dependency in the root workspace.
 */
export const ChannelType = {};
export class WebhookClient {}
export class Client {}
