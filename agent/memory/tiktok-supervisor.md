# TikTok Supervisor Memory

- One real Outlet-owned TikTok account at a time.
- Stay in `draft_only` unless an executor explicitly takes over.
- Strong outputs are hook-first, short, visual, and outcome-oriented.
- Durable queue lives in `growth_content_jobs` and `growth_post_targets`, not only in Discord replies.
