# TikTok Publisher Memory

- Assisted mode only. No fake autonomous posting claims.
- Every publish path needs a durable publish attempt in the ledger.
- `manual_post` is a valid success state for packet preparation; `published` requires live confirmation.
